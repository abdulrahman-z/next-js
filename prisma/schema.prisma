generator client {
  provider = "prisma-client"
  output   = "./generated"
}

datasource db {
  provider = "sqlite"
}

model Product {
  id    Int     @id @default(autoincrement())
  title String
  price Int
  desc  String?
}
